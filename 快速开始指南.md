# GB28181 功能模块快速开始指南

## 🚀 5分钟快速开始

### 前提条件
- ✅ 已安装 Visual Studio 2017+ 或 GCC 7+
- ✅ 已安装 CMake 3.8+
- ✅ 已安装 oatpp 和相关依赖

---

## 📦 步骤1: 集成代码

### 1.1 复制文件到项目
将以下新文件复制到对应目录：

```bash
GBServer/GbSipServer/
├── sip/
│   ├── SipMessageHandler.hpp
│   ├── SipMessageHandler.cpp
│   ├── CatalogHandler.hpp
│   └── CatalogHandler.cpp
├── utils/
│   ├── XmlHelper.hpp
│   └── XmlHelper.cpp
├── service/
│   ├── CatalogService.hpp
│   └── CatalogService.cpp
├── controller/
│   └── CatalogController.hpp
├── dto/
│   ├── ChannelDto.hpp
│   └── CatalogDto.hpp
├── db/
│   └── ChannelDb.hpp
└── sql/
    └── create_channel_table.sql
```

### 1.2 修改 sip_server.h

在文件开头添加：
```cpp
#include "sip/CatalogHandler.hpp"
```

在 SipServer 类中添加：
```cpp
private:
    std::shared_ptr<gbsip_server::CatalogHandler> catalog_handler_;

public:
    std::shared_ptr<gbsip_server::CatalogHandler> getCatalogHandler() {
        return catalog_handler_;
    }
    
    std::shared_ptr<SipClient> getDeviceInfo(const std::string& deviceId);
```

### 1.3 修改 sip_server.cpp

在 `init()` 方法末尾添加：
```cpp
// 初始化CatalogHandler
catalog_handler_ = std::make_shared<gbsip_server::CatalogHandler>(
    sip_context_,
    sip_server_info_.sipServerId,
    sip_server_info_.ip,
    sip_server_info_.port
);

// 设置目录通知回调
catalog_handler_->setNotifyCallback(
    [this](const std::string& deviceId, 
           const std::vector<gbsip_server::ChannelInfo>& channels) {
        SIPSERVER_LOG(LS_INFO) << "Received catalog notify from device: " 
                               << deviceId 
                               << ", channels: " << channels.size();
    }
);
```

修改 `HandlerSipMessageAnswerd()` 方法：
```cpp
void SipServer::HandlerSipMessageAnswerd(eXosip_event_t * sip_event) {
    if (!sip_event || !sip_event->request) {
        return;
    }
    
    osip_body_t* body = nullptr;
    osip_message_get_body(sip_event->request, 0, &body);
    if (!body || !body->body) {
        return;
    }
    
    std::string xml(body->body);
    auto root = gbsip_server::XmlHelper::parseXml(xml);
    if (!root) {
        return;
    }
    
    std::string cmdType = gbsip_server::XmlHelper::getNodeValue(root, "Response/CmdType");
    
    if (cmdType == "Catalog") {
        if (catalog_handler_) {
            catalog_handler_->handleMessageResponse(sip_event);
        }
    }
}
```

添加 `getDeviceInfo()` 方法：
```cpp
std::shared_ptr<SipClient> SipServer::getDeviceInfo(const std::string& deviceId) {
    auto it = client_map_.find(deviceId);
    if (it != client_map_.end()) {
        return it->second;
    }
    return nullptr;
}
```

### 1.4 修改 CatalogService.cpp

添加头文件：
```cpp
#include "sip_server.h"
#include <future>
```

修改 `queryCatalog()` 方法（参考集成指南.md中的完整代码）

### 1.5 修改 DatabaseComponent.hpp

添加：
```cpp
#include "db/ChannelDb.hpp"

OATPP_CREATE_COMPONENT(std::shared_ptr<ChannelDb>, channelDb)([] {
    OATPP_COMPONENT(std::shared_ptr<oatpp::orm::Executor>, executor);
    return std::make_shared<ChannelDb>(executor);
}());
```

### 1.6 修改 main.cpp

添加：
```cpp
#include "controller/CatalogController.hpp"

// 在 main() 函数中添加
router->addController(CatalogController::createShared());
```

### 1.7 更新 CMakeLists.txt

添加源文件：
```cmake
set(GBSIPSERVER_SOURCES
    # ... 现有文件 ...
    sip/SipMessageHandler.cpp
    sip/CatalogHandler.cpp
    service/CatalogService.cpp
    utils/XmlHelper.cpp
)
```

添加包含目录：
```cmake
include_directories(
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_CURRENT_SOURCE_DIR}/sip
    # ... 其他目录 ...
)
```

---

## 🔨 步骤2: 编译项目

```bash
cd GBServer
mkdir -p build
cd build
cmake ..
cmake --build . --config Release
```

如果编译成功，你会看到：
```
[100%] Built target GbSipServer
```

---

## ▶️ 步骤3: 运行服务器

```bash
# Windows
.\bin\Release\GbSipServer.exe ..\config\gbsip_server.yaml

# Linux
./bin/GbSipServer ../config/gbsip_server.yaml
```

看到以下日志表示启动成功：
```
[INFO] Sip ServerStart ...
[INFO] HTTP server started on port 8003
```

---

## 🧪 步骤4: 测试功能

### 4.1 查看Swagger文档
打开浏览器访问：
```
http://localhost:8003/swagger/ui
```

### 4.2 测试目录查询API

**方法1: 使用curl**
```bash
curl http://localhost:8003/api/v1/device/34020000001320000001/catalog
```

**方法2: 使用Postman**
```
GET http://localhost:8003/api/v1/device/34020000001320000001/catalog
```

**预期响应**（如果设备在线）：
```json
{
  "device_id": "34020000001320000001",
  "sn": 1,
  "sum_num": 2,
  "channels": [
    {
      "channel_id": "34020000001320000002",
      "device_id": "34020000001320000001",
      "name": "Camera 1",
      "manufacturer": "Hikvision",
      "status": "ON",
      "ptz_type": 1
    }
  ]
}
```

### 4.3 测试通道列表查询
```bash
curl http://localhost:8003/api/v1/device/34020000001320000001/channels
```

### 4.4 测试目录订阅
```bash
curl -X POST http://localhost:8003/api/v1/catalog/subscribe \
  -H "Content-Type: application/json" \
  -d '{
    "device_id": "34020000001320000001",
    "expires": 3600
  }'
```

---

## 🔍 步骤5: 查看日志

日志会输出到控制台，关键日志包括：

```
[INFO] Sent MESSAGE to device: 34020000001320000001, SN: 1
[INFO] Received MESSAGE response, SN: 1
[INFO] Received catalog response from device: 34020000001320000001, channels: 2
```

---

## 🐛 常见问题

### 问题1: 编译错误 - 找不到头文件
**解决方案**：
- 检查 CMakeLists.txt 中的 include_directories
- 确保所有新文件都已复制到正确位置

### 问题2: 链接错误
**解决方案**：
- 检查 CMakeLists.txt 中的源文件列表
- 确保所有 .cpp 文件都已添加

### 问题3: 运行时错误 - 设备未找到
**解决方案**：
- 确保设备已注册（查看 t_device 表）
- 检查设备ID是否正确

### 问题4: 查询超时
**解决方案**：
- 检查设备是否在线
- 检查网络连接
- 查看SIP日志确认消息是否发送

### 问题5: 数据库错误
**解决方案**：
- 检查 DatabaseComponent.hpp 中的 ChannelDb 注册
- 确保数据库文件有写权限

---

## 📊 验证清单

完成以下检查确保集成成功：

- [ ] 编译无错误
- [ ] 服务器启动成功
- [ ] Swagger文档可访问
- [ ] 目录查询API返回200
- [ ] 通道列表API返回数据
- [ ] 日志中有SIP消息记录
- [ ] 数据库中有通道记录

---

## 📚 下一步

### 学习更多
- 阅读 `GB28181_功能完善度评估与规划.md` 了解完整规划
- 阅读 `实现思路.md` 了解技术细节
- 阅读 `集成指南.md` 了解详细集成步骤

### 继续开发
- 实现 Sprint 2: 设备信息查询
- 实现 Sprint 3: 云台控制PTZ
- 实现 Sprint 4: 录像管理

### 测试和优化
- 运行单元测试
- 进行性能测试
- 优化数据库查询
- 添加缓存机制

---

## 🆘 获取帮助

### 文档
- `README_实现说明.md` - 完整实现说明
- `实现完成总结.md` - 实现总结
- `SIP处理层实现思路.md` - SIP处理层设计

### 调试技巧
1. 启用详细日志：修改日志级别为 LS_VERBOSE
2. 使用Wireshark抓包分析SIP消息
3. 检查数据库内容：`sqlite3 db.sqlite "SELECT * FROM t_channel;"`
4. 使用Postman测试API

### 联系方式
- 项目地址：https://github.com/chensongpoixs/GBServer
- 作者：chensong

---

## 🎉 恭喜！

如果你完成了以上所有步骤，你已经成功集成了GB28181设备目录管理功能！

现在你可以：
- ✅ 查询设备目录
- ✅ 订阅目录变化
- ✅ 管理通道信息
- ✅ 通过HTTP API访问

继续加油，完成更多GB28181功能！💪

---

**文档版本**: v1.0  
**创建日期**: 2026-02-09  
**适用版本**: Sprint 1 完整版
