# GB28181 功能模块实现思路

## 一、实现策略

### 1.1 分阶段实现
按照优先级从高到低，分三个阶段实现：
- **阶段一（Sprint 1-4）**: 核心功能 - 目录管理、设备查询、PTZ控制、录像管理
- **阶段二（Sprint 5-7）**: 扩展功能 - 报警、语音、级联
- **阶段三（Sprint 8-10）**: 高级功能 - 位置、配置、控制

### 1.2 模块化设计原则
- 每个功能模块独立封装
- 统一的错误处理机制
- 统一的日志记录
- 统一的XML消息处理

## 二、技术架构设计

### 2.1 目录结构规划

```
GBServer/GbSipServer/
├── controller/              # HTTP API控制器层
│   ├── CatalogController.hpp       # 目录管理
│   ├── PTZController.hpp           # 云台控制
│   ├── RecordController.hpp        # 录像管理
│   ├── AlarmController.hpp         # 报警管理
│   └── ...
├── service/                 # 业务逻辑层
│   ├── CatalogService.hpp/cpp
│   ├── ChannelService.hpp/cpp
│   ├── PTZService.hpp/cpp
│   ├── RecordService.hpp/cpp
│   └── ...
├── dto/                     # 数据传输对象
│   ├── ChannelDto.hpp
│   ├── PTZControlDto.hpp
│   ├── RecordDto.hpp
│   └── ...
├── db/                      # 数据库访问层
│   ├── ChannelDb.hpp
│   ├── RecordDb.hpp
│   └── ...
├── sip/                     # SIP信令处理层（新增）
│   ├── SipMessageHandler.hpp/cpp   # 基类
│   ├── CatalogHandler.hpp/cpp      # 目录查询处理
│   ├── PTZHandler.hpp/cpp          # PTZ控制处理
│   ├── RecordHandler.hpp/cpp       # 录像查询处理
│   └── ...
└── utils/                   # 工具类
    ├── XmlParser.hpp/cpp           # XML解析
    ├── XmlBuilder.hpp/cpp          # XML构建
    └── GB28181XmlHelper.hpp/cpp    # GB28181 XML辅助
```

### 2.2 数据流设计

```
HTTP请求 → Controller → Service → SipHandler → eXosip → 设备
                ↓           ↓
              DTO         Database
                ↓
           HTTP响应
```

## 三、第一阶段实现计划（Sprint 1: 设备目录管理）

### 3.1 数据库设计

#### 3.1.1 通道信息表
```sql
CREATE TABLE t_channel (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    channel_id VARCHAR(20) UNIQUE NOT NULL,
    device_id VARCHAR(20) NOT NULL,
    channel_name VARCHAR(255),
    manufacturer VARCHAR(100),
    model VARCHAR(100),
    owner VARCHAR(100),
    civil_code VARCHAR(6),
    address VARCHAR(255),
    parental INTEGER DEFAULT 0,
    parent_id VARCHAR(20),
    safety_way INTEGER DEFAULT 0,
    register_way INTEGER DEFAULT 1,
    secrecy INTEGER DEFAULT 0,
    status VARCHAR(10) DEFAULT 'OFF',
    longitude REAL DEFAULT 0,
    latitude REAL DEFAULT 0,
    ptz_type INTEGER DEFAULT 0,
    create_time INTEGER,
    update_time INTEGER,
    FOREIGN KEY (device_id) REFERENCES t_device(device_id)
);
```

### 3.2 核心类设计

#### 3.2.1 ChannelDto.hpp
```cpp
class ChannelDto : public oatpp::DTO {
    DTO_INIT(ChannelDto, DTO)
    
    DTO_FIELD(String, channelId, "channel_id");
    DTO_FIELD(String, deviceId, "device_id");
    DTO_FIELD(String, name, "name");
    DTO_FIELD(String, manufacturer, "manufacturer");
    DTO_FIELD(String, model, "model");
    DTO_FIELD(String, owner, "owner");
    DTO_FIELD(String, civilCode, "civil_code");
    DTO_FIELD(String, address, "address");
    DTO_FIELD(Int32, parental, "parental");
    DTO_FIELD(String, parentId, "parent_id");
    DTO_FIELD(String, status, "status");
    DTO_FIELD(Float64, longitude, "longitude");
    DTO_FIELD(Float64, latitude, "latitude");
    DTO_FIELD(Int32, ptzType, "ptz_type");
};
```

#### 3.2.2 CatalogService.hpp
```cpp
class CatalogService {
public:
    // 查询设备目录
    oatpp::Object<CatalogDto> queryCatalog(const String& deviceId);
    
    // 订阅目录
    oatpp::Object<StatusDto> subscribeCatalog(const String& deviceId);
    
    // 取消订阅
    oatpp::Object<StatusDto> unsubscribeCatalog(const String& deviceId);
    
    // 处理目录通知
    void handleCatalogNotify(const String& deviceId, const String& xml);
};
```

#### 3.2.3 CatalogHandler.hpp
```cpp
class CatalogHandler : public SipMessageHandler {
public:
    // 发送目录查询请求
    int32_t sendCatalogQuery(const String& deviceId);
    
    // 处理目录查询响应
    void handleCatalogResponse(eXosip_event_t* event);
    
    // 发送目录订阅
    int32_t sendCatalogSubscribe(const String& deviceId);
    
    // 处理目录通知
    void handleCatalogNotify(eXosip_event_t* event);
};
```

### 3.3 XML消息格式

#### 3.3.1 目录查询请求
```xml
<?xml version="1.0" encoding="GB2312"?>
<Query>
    <CmdType>Catalog</CmdType>
    <SN>1</SN>
    <DeviceID>34020000001320000001</DeviceID>
</Query>
```

#### 3.3.2 目录查询响应
```xml
<?xml version="1.0" encoding="GB2312"?>
<Response>
    <CmdType>Catalog</CmdType>
    <SN>1</SN>
    <DeviceID>34020000001320000001</DeviceID>
    <SumNum>2</SumNum>
    <DeviceList Num="2">
        <Item>
            <DeviceID>34020000001320000002</DeviceID>
            <Name>Camera 1</Name>
            <Manufacturer>Hikvision</Manufacturer>
            <Model>DS-2CD2T47EWD-L</Model>
            <Owner>Owner</Owner>
            <CivilCode>340200</CivilCode>
            <Address>Address</Address>
            <Parental>0</Parental>
            <ParentID>34020000001320000001</ParentID>
            <SafetyWay>0</SafetyWay>
            <RegisterWay>1</RegisterWay>
            <Secrecy>0</Secrecy>
            <Status>ON</Status>
        </Item>
    </DeviceList>
</Response>
```

### 3.4 实现步骤

#### 步骤1: 创建数据库表和DTO
1. 创建 t_channel 表
2. 创建 ChannelDto.hpp
3. 创建 CatalogDto.hpp

#### 步骤2: 创建数据库访问层
1. 创建 ChannelDb.hpp
2. 实现 CRUD 操作

#### 步骤3: 创建XML工具类
1. 创建 XmlParser.hpp/cpp
2. 创建 XmlBuilder.hpp/cpp
3. 创建 GB28181XmlHelper.hpp/cpp

#### 步骤4: 创建SIP处理层
1. 创建 SipMessageHandler.hpp/cpp（基类）
2. 创建 CatalogHandler.hpp/cpp
3. 实现目录查询和订阅

#### 步骤5: 创建Service层
1. 创建 CatalogService.hpp/cpp
2. 创建 ChannelService.hpp/cpp
3. 实现业务逻辑

#### 步骤6: 创建Controller层
1. 创建 CatalogController.hpp
2. 实现 HTTP API 接口

#### 步骤7: 集成到SipServer
1. 在 sip_server.cpp 中注册 CatalogHandler
2. 处理 MESSAGE 事件中的 Catalog 响应

## 四、关键技术点

### 4.1 XML解析策略
使用轻量级XML解析库（如 tinyxml2 或 pugixml）：
- 解析设备响应的XML
- 构建发送给设备的XML
- 处理GB2312编码

### 4.2 SIP消息发送
```cpp
// 发送MESSAGE消息
int sendMessage(const String& deviceId, const String& xml) {
    osip_message_t* message = nullptr;
    
    // 构建MESSAGE请求
    eXosip_message_build_request(&message, "MESSAGE", 
        to_uri, from_uri, nullptr);
    
    // 设置Content-Type
    osip_message_set_content_type(message, "Application/MANSCDP+xml");
    
    // 设置消息体
    osip_message_set_body(message, xml.c_str(), xml.length());
    
    // 发送消息
    eXosip_message_send_request(sip_context_, message);
}
```

### 4.3 异步响应处理
```cpp
// 使用回调机制处理异步响应
class CatalogQueryCallback {
public:
    virtual void onSuccess(const CatalogDto& catalog) = 0;
    virtual void onError(int code, const String& message) = 0;
};

// 注册回调
void queryCatalogAsync(const String& deviceId, 
                       std::shared_ptr<CatalogQueryCallback> callback);
```

### 4.4 超时处理
```cpp
// 设置查询超时（30秒）
const int QUERY_TIMEOUT = 30;

// 使用定时器检测超时
void startQueryTimer(const String& sn) {
    auto timer = std::make_shared<Timer>();
    timer->start(QUERY_TIMEOUT, [sn, this]() {
        handleQueryTimeout(sn);
    });
}
```

## 五、测试策略

### 5.1 单元测试
- 测试XML解析和构建
- 测试数据库操作
- 测试Service层业务逻辑

### 5.2 集成测试
- 使用GB28181模拟器测试
- 测试与真实设备对接
- 测试异常情况处理

### 5.3 性能测试
- 测试1000个通道的查询性能
- 测试并发查询性能
- 测试内存占用

## 六、下一步行动

### 立即开始（本次实现）
1. ✅ 创建数据库表结构
2. ✅ 创建基础DTO类
3. ✅ 创建XML工具类
4. ✅ 实现目录查询功能
5. ✅ 创建API接口

### 后续实现
- Sprint 2: 设备信息查询
- Sprint 3: PTZ控制
- Sprint 4: 录像管理

---

**开始实现时间**: 2026-02-09  
**预计完成时间**: Sprint 1 - 2周
