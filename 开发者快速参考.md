# GB28181 SIP服务器 - 开发者快速参考

## 🚀 5分钟快速开始

### 1. 编译运行
```bash
cd GBServer/GbSipServer/build
cmake ..
make -j4
./bin/GbSipServer ../config/gbsip_server.yaml
```

### 2. 访问API文档
```
http://localhost:8003/swagger/ui
```

### 3. 测试API
```bash
# 查询设备列表
curl http://localhost:8003/devices/offset/0/limit/10

# PTZ向上移动
curl -X POST http://localhost:8003/api/v1/ptz/control \
  -H "Content-Type: application/json" \
  -d '{"device_id":"34020000001320000001","channel_id":"34020000001320000002","direction":"UP","h_speed":128,"v_speed":128}'
```

---

## 📚 核心概念

### GB28181设备ID格式
```
34020000001320000001
││││││││││││││││││││
│└─┴─┴─┴─┴─┴─┴─┴─┴─┴─ 设备序号 (10位)
└─┴─┴─┴─┴─┴─┴─┴─┴─── 类型编码 (3位)
  └─┴─┴─┴─┴─┴─┴───── 行业编码 (2位)
    └─┴─┴─┴─┴─┴───── 中心编码-省市县 (8位)
```

### SIP消息类型
| 消息类型 | 用途 | 示例 |
|---------|------|------|
| REGISTER | 设备注册 | 设备上线 |
| MESSAGE | 查询请求 | 目录查询、设备信息查询 |
| INFO | 控制命令 | PTZ控制、回放控制 |
| INVITE | 媒体会话 | 实时视频、录像回放 |
| SUBSCRIBE | 订阅请求 | 目录订阅、报警订阅 |
| NOTIFY | 通知消息 | 目录通知、报警通知 |

---

## 🔧 常用代码片段

### 1. 添加新的API接口

**步骤1: 创建DTO**
```cpp
// dto/MyDto.hpp
class MyRequestDto : public oatpp::DTO {
    DTO_INIT(MyRequestDto, DTO)
    DTO_FIELD(String, deviceId, "device_id");
    DTO_FIELD(String, param1);
};
```

**步骤2: 创建Service**
```cpp
// service/MyService.hpp
class MyService {
public:
    oatpp::Object<MyResponseDto> doSomething(
        const oatpp::Object<MyRequestDto>& dto);
};
```

**步骤3: 创建Controller**
```cpp
// controller/MyController.hpp
ENDPOINT("POST", "/api/v1/my/endpoint", myEndpoint,
         BODY_DTO(Object<MyRequestDto>, dto))
{
    auto result = m_myService.doSomething(dto);
    return createDtoResponse(Status::CODE_200, result);
}
```

**步骤4: 注册Controller**
```cpp
// main.cpp
router->addController(MyController::createShared());
```

---

### 2. 发送SIP MESSAGE

```cpp
// 构建XML
std::string xml = "<?xml version=\"1.0\" encoding=\"GB2312\"?>\r\n"
                  "<Query>\r\n"
                  "    <CmdType>DeviceInfo</CmdType>\r\n"
                  "    <SN>123</SN>\r\n"
                  "    <DeviceID>34020000001320000001</DeviceID>\r\n"
                  "</Query>\r\n";

// 发送MESSAGE
osip_message_t* msg = nullptr;
eXosip_message_build_request(context_, &msg, "MESSAGE", to, from, nullptr);
osip_message_set_body(msg, xml.c_str(), xml.length());
osip_message_set_content_type(msg, "Application/MANSCDP+xml");
eXosip_message_send_request(context_, msg);
```

---

### 3. 发送SIP INFO（PTZ控制）

```cpp
// 构建PTZ XML
std::string xml = "<?xml version=\"1.0\" encoding=\"GB2312\"?>\r\n"
                  "<Control>\r\n"
                  "    <CmdType>DeviceControl</CmdType>\r\n"
                  "    <SN>124</SN>\r\n"
                  "    <DeviceID>34020000001320000002</DeviceID>\r\n"
                  "    <PTZCmd>A50F95000000000000</PTZCmd>\r\n"
                  "</Control>\r\n";

// 发送INFO
osip_message_t* info = nullptr;
eXosip_message_build_request(context_, &info, "INFO", to, from, nullptr);
osip_message_set_body(info, xml.c_str(), xml.length());
osip_message_set_content_type(info, "Application/MANSCDP+xml");
eXosip_message_send_request(context_, info);
```

---

### 4. 解析XML响应

```cpp
// 使用XmlHelper解析
auto root = XmlHelper::parseXml(xml);
std::string cmdType = XmlHelper::getNodeValue(root, "Response/CmdType");
std::string deviceId = XmlHelper::getNodeValue(root, "Response/DeviceID");

// 获取子节点列表
auto items = XmlHelper::getChildNodes(root, "Response/RecordList/Item");
for (const auto& item : items) {
    std::string name = XmlHelper::getNodeValue(item, "Name");
    std::string startTime = XmlHelper::getNodeValue(item, "StartTime");
}
```

---

### 5. 数据库操作

```cpp
// 查询设备
auto device = device_db_->getDeviceById("34020000001320000001");

// 创建设备
auto deviceDto = DeviceDto::createShared();
deviceDto->deviceID = "34020000001320000001";
deviceDto->RemoteIP = "192.168.1.100";
deviceDto->RemotePort = 5060;
device_db_->createDevice(deviceDto);

// 更新设备
device_db_->updateDevice(deviceDto);

// 删除设备
device_db_->deleteDeviceById("34020000001320000001");
```

---

## 📖 API快速参考

### 设备管理
```bash
# 创建设备
POST /devices
{"deviceID":"34020000001320000001","RemoteIP":"192.168.1.100","RemotePort":5060}

# 查询设备
GET /devices/34020000001320000001

# 分页查询
GET /devices/offset/0/limit/10

# 更新设备
PUT /device/34020000001320000001

# 删除设备
DELETE /devices/34020000001320000001
```

### 设备目录
```bash
# 查询设备目录
GET /api/v1/device/34020000001320000001/catalog

# 获取通道列表
GET /api/v1/device/34020000001320000001/channels

# 订阅目录
POST /api/v1/catalog/subscribe
{"device_id":"34020000001320000001","expires":3600}
```

### 设备信息
```bash
# 查询设备信息
GET /api/v1/device/34020000001320000001/info

# 查询设备状态
GET /api/v1/device/34020000001320000001/status
```

### PTZ控制
```bash
# 方向控制
POST /api/v1/ptz/control
{"device_id":"...","channel_id":"...","direction":"UP","h_speed":128,"v_speed":128}

# 变倍控制
POST /api/v1/ptz/zoom
{"device_id":"...","channel_id":"...","zoom_in":true,"speed":128}

# 预置位调用
POST /api/v1/ptz/preset/call
{"device_id":"...","channel_id":"...","preset_id":1}
```

---

## 🔍 调试技巧

### 1. 查看SIP消息
```cpp
// 在sip_server.cpp中启用详细日志
void SipServer::request_info(eXosip_event_t * sip_event) {
    char *info;
    size_t len = 0;
    osip_message_to_str(sip_event->request, &info, &len);
    SIPSERVER_LOG(LS_INFO) << "SIP Request:\n" << info;
}
```

### 2. 使用Wireshark抓包
```bash
# 过滤SIP消息
sip

# 过滤特定端口
udp.port == 5060

# 过滤特定IP
ip.addr == 192.168.1.100
```

### 3. 查看数据库
```bash
sqlite3 db/gbsip.db
.tables
SELECT * FROM devices;
SELECT * FROM channels;
```

---

## 🐛 常见问题

### 1. 设备注册失败
**问题**: 设备发送REGISTER但收不到响应

**检查**:
- 服务器IP和端口配置是否正确
- 防火墙是否开放5060端口
- 设备ID和密码是否匹配

**解决**:
```yaml
# config/gbsip_server.yaml
sip:
  server_id: "34020000002000000001"
  server_ip: "0.0.0.0"  # 监听所有网卡
  server_port: 5060
  password: "12345678"
```

---

### 2. PTZ控制无响应
**问题**: 发送PTZ命令但设备不动作

**检查**:
- 设备是否在线
- 通道ID是否正确
- PTZ命令编码是否正确

**调试**:
```cpp
// 打印PTZ命令
SIPSERVER_LOG(LS_INFO) << "PTZ Command: " << ptzCmd;
// 应该输出类似: A50F95000000000000
```

---

### 3. XML解析失败
**问题**: 收到XML响应但解析失败

**检查**:
- XML编码是否为GB2312
- XML格式是否正确
- 是否有特殊字符

**解决**:
```cpp
// 打印原始XML
SIPSERVER_LOG(LS_INFO) << "Raw XML:\n" << xml;

// 检查编码
if (xml.find("GB2312") == std::string::npos) {
    SIPSERVER_LOG(LS_WARNING) << "XML encoding is not GB2312";
}
```

---

## 📊 性能优化

### 1. 数据库索引
```sql
-- 为常用查询字段添加索引
CREATE INDEX idx_device_id ON devices(deviceID);
CREATE INDEX idx_channel_device ON channels(device_id);
CREATE INDEX idx_time_range ON records(start_time, end_time);
```

### 2. 连接池配置
```cpp
// AppComponent.hpp
OATPP_CREATE_COMPONENT(std::shared_ptr<oatpp::provider::Provider<oatpp::sqlite::Connection>>, dbConnectionProvider)([] {
    auto connectionProvider = std::make_shared<oatpp::sqlite::ConnectionProvider>(DATABASE_FILE);
    return oatpp::sqlite::ConnectionPool::createShared(
        connectionProvider,
        10,  // 最大连接数
        std::chrono::seconds(5)  // 连接超时
    );
}());
```

### 3. 异步处理
```cpp
// 使用Promise/Future实现异步
std::promise<Result> promise;
auto future = promise.get_future();

handler->doSomething([&promise](const Result& result) {
    promise.set_value(result);
});

// 等待结果（带超时）
if (future.wait_for(std::chrono::seconds(10)) == std::future_status::timeout) {
    // 超时处理
}
```

---

## 🔐 安全建议

### 1. SIP认证
```cpp
// 使用强密码
sip_server_info_.SipSeverPass = "ComplexPassword123!";

// 定期更新nonce
std::string generateNonce() {
    return std::to_string(std::time(nullptr)) + 
           std::to_string(rand());
}
```

### 2. API访问控制
```cpp
// 添加JWT认证（计划中）
ENDPOINT("POST", "/api/v1/protected", protectedEndpoint,
         AUTHORIZATION(std::shared_ptr<DefaultBearerAuthorizationObject>, authObject))
{
    // 验证token
    if (!authObject || !authObject->token) {
        return createResponse(Status::CODE_401, "Unauthorized");
    }
}
```

### 3. 输入验证
```cpp
// 验证设备ID格式
bool isValidDeviceId(const std::string& deviceId) {
    return deviceId.length() == 20 && 
           std::all_of(deviceId.begin(), deviceId.end(), ::isdigit);
}
```

---

## 📚 参考资源

### 官方文档
- [GB/T 28181-2016](http://www.gb688.cn/bzgk/gb/newGbInfo?hcno=469659DC56B9B8187671FF08748CEC89)
- [eXosip2 Documentation](http://savannah.nongnu.org/projects/exosip)
- [oatpp Documentation](https://oatpp.io/docs/start/)

### 项目文档
- [README.md](README.md) - 项目说明
- [架构设计文档.md](架构设计文档.md) - 系统架构
- [快速开始指南.md](快速开始指南.md) - 快速入门

### 示例代码
- [CatalogHandler.cpp](sip/CatalogHandler.cpp) - SIP处理示例
- [PTZService.cpp](service/PTZService.cpp) - Service层示例
- [PTZController.hpp](controller/PTZController.hpp) - Controller层示例

---

## 💡 最佳实践

### 1. 代码组织
```
每个功能模块包含:
- DTO: 数据传输对象
- Handler: SIP消息处理
- Service: 业务逻辑
- Controller: HTTP API
- 文档: 实现说明和集成指南
```

### 2. 错误处理
```cpp
try {
    // 业务逻辑
} catch (const std::exception& e) {
    SIPSERVER_LOG(LS_ERROR) << "Error: " << e.what();
    return createErrorResponse(500, e.what());
}
```

### 3. 日志记录
```cpp
// 使用统一的日志宏
SIPSERVER_LOG(LS_INFO) << "Operation success";
SIPSERVER_LOG(LS_WARNING) << "Warning message";
SIPSERVER_LOG(LS_ERROR) << "Error occurred";
```

---

**最后更新**: 2026-02-09  
**版本**: v1.0  
**维护者**: AI Assistant
