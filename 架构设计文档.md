# GB28181 SIP服务器架构设计文档

## 📐 系统架构概览

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端层                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Web浏览器   │  │  移动APP     │  │  第三方系统   │          │
│  │  (Swagger UI)│  │              │  │              │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                 │                 │                   │
│         └─────────────────┴─────────────────┘                   │
│                           │                                     │
│                      HTTP/REST API                              │
└───────────────────────────┼─────────────────────────────────────┘
                            │
┌───────────────────────────┼─────────────────────────────────────┐
│                    GB28181 SIP服务器                             │
│                            │                                     │
│  ┌─────────────────────────▼──────────────────────────────┐    │
│  │              HTTP API层 (oatpp)                         │    │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │    │
│  │  │  User    │ │  Device  │ │  Catalog │ │   PTZ    │  │    │
│  │  │Controller│ │Controller│ │Controller│ │Controller│  │    │
│  │  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘  │    │
│  └───────┼────────────┼────────────┼────────────┼────────┘    │
│          │            │            │            │              │
│  ┌───────▼────────────▼────────────▼────────────▼────────┐    │
│  │              Service层 (业务逻辑)                       │    │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │    │
│  │  │  User    │ │  Device  │ │  Catalog │ │   PTZ    │  │    │
│  │  │ Service  │ │ Service  │ │ Service  │ │ Service  │  │    │
│  │  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘  │    │
│  └───────┼────────────┼────────────┼────────────┼────────┘    │
│          │            │            │            │              │
│  ┌───────▼────────────▼────────────▼────────────▼────────┐    │
│  │              SIP Handler层 (SIP消息处理)                │    │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │    │
│  │  │ Message  │ │  Catalog │ │DeviceInfo│ │   PTZ    │  │    │
│  │  │ Handler  │ │ Handler  │ │ Handler  │ │ Handler  │  │    │
│  │  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘  │    │
│  └───────┼────────────┼────────────┼────────────┼────────┘    │
│          │            │            │            │              │
│  ┌───────▼────────────▼────────────▼────────────▼────────┐    │
│  │              SIP协议栈 (eXosip2)                        │    │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │    │
│  │  │ REGISTER │ │ MESSAGE  │ │  INFO    │ │  INVITE  │  │    │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │    │
│  └─────────────────────────────────────────────────────────┘    │
│                            │                                     │
│  ┌─────────────────────────▼──────────────────────────────┐    │
│  │              数据库层 (SQLite3)                          │    │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │    │
│  │  │  User    │ │  Device  │ │  Channel │ │  Record  │  │    │
│  │  │   DB     │ │    DB    │ │    DB    │ │    DB    │  │    │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ SIP/UDP
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                    GB28181设备层                                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │  IPC摄像头│ │  NVR设备 │ │  DVR设备 │ │  其他设备 │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🏗️ 分层架构设计

### 1. HTTP API层

**职责**:
- 接收HTTP请求
- 参数验证
- 调用Service层
- 返回HTTP响应
- 生成Swagger文档

**技术**:
- oatpp Web框架
- oatpp-swagger文档生成
- RESTful API设计

**主要组件**:
```cpp
UserController       // 用户管理API
DeviceController     // 设备管理API
CatalogController    // 目录管理API
DeviceInfoController // 设备信息API
PTZController        // PTZ控制API
RecordController     // 录像管理API (待实现)
AlarmController      // 报警管理API (待实现)
```

---

### 2. Service层

**职责**:
- 业务逻辑处理
- 数据验证
- 调用SIP Handler
- 调用数据库
- 错误处理

**设计模式**:
- 单一职责原则
- 依赖注入
- 异步处理

**主要组件**:
```cpp
CatalogService       // 目录服务
DeviceInfoService    // 设备信息服务
PTZService           // PTZ控制服务
RecordService        // 录像服务 (待实现)
AlarmService         // 报警服务 (待实现)
```

---

### 3. SIP Handler层

**职责**:
- 构建SIP消息
- 发送SIP请求
- 处理SIP响应
- XML消息处理
- 异步回调管理

**消息类型**:
- REGISTER - 设备注册
- MESSAGE - 目录查询、设备信息查询
- INFO - PTZ控制
- INVITE - 实时视频、录像回放
- SUBSCRIBE - 目录订阅、报警订阅
- NOTIFY - 目录通知、报警通知

**主要组件**:
```cpp
SipMessageHandler    // SIP消息处理基类
CatalogHandler       // 目录查询处理器
DeviceInfoHandler    // 设备信息处理器
PTZHandler           // PTZ控制处理器
RecordHandler        // 录像处理器 (待实现)
AlarmHandler         // 报警处理器 (待实现)
```

---

### 4. SIP协议栈层

**职责**:
- SIP协议实现
- UDP/TCP传输
- 事务管理
- 对话管理

**技术**:
- eXosip2库
- osip2库

**主要功能**:
```cpp
eXosip_init()                    // 初始化
eXosip_listen_addr()             // 监听端口
eXosip_message_build_request()  // 构建请求
eXosip_message_send_request()   // 发送请求
eXosip_event_wait()              // 等待事件
```

---

### 5. 数据库层

**职责**:
- 数据持久化
- CRUD操作
- 事务管理
- 查询优化

**技术**:
- SQLite3
- oatpp-sqlite ORM

**数据表**:
```sql
users       // 用户表
devices     // 设备表
channels    // 通道表
records     // 录像表 (待实现)
alarms      // 报警表 (待实现)
```

---

## 🔄 数据流设计

### 1. PTZ控制流程

```
┌─────────┐
│ 客户端  │
└────┬────┘
     │ HTTP POST /api/v1/ptz/control
     ▼
┌─────────────┐
│PTZController│
└─────┬───────┘
      │ controlDirection(dto)
      ▼
┌─────────────┐
│ PTZService  │
└─────┬───────┘
      │ 1. 参数验证
      │ 2. 查询设备信息
      │ 3. 编码PTZ命令
      ▼
┌─────────────┐
│ PTZHandler  │
└─────┬───────┘
      │ 1. 构建XML
      │ 2. 发送INFO消息
      ▼
┌─────────────┐
│   eXosip2   │
└─────┬───────┘
      │ SIP INFO
      ▼
┌─────────────┐
│  GB28181设备│
└─────┬───────┘
      │ 200 OK
      ▼
┌─────────────┐
│   eXosip2   │
└─────┬───────┘
      │ 事件回调
      ▼
┌─────────────┐
│ PTZHandler  │
└─────┬───────┘
      │ 成功回调
      ▼
┌─────────────┐
│ PTZService  │
└─────┬───────┘
      │ 返回结果
      ▼
┌─────────────┐
│PTZController│
└─────┬───────┘
      │ HTTP 200 OK
      ▼
┌─────────┐
│ 客户端  │
└─────────┘
```

---

### 2. 设备注册流程

```
┌─────────────┐
│  GB28181设备│
└─────┬───────┘
      │ SIP REGISTER (无认证)
      ▼
┌─────────────┐
│   eXosip2   │
└─────┬───────┘
      │ EXOSIP_MESSAGE_NEW
      ▼
┌─────────────┐
│  SipServer  │
└─────┬───────┘
      │ HandlerSipMessageNew()
      │ 1. 检查认证信息
      │ 2. 生成401响应
      ▼
┌─────────────┐
│   eXosip2   │
└─────┬───────┘
      │ SIP 401 Unauthorized
      ▼
┌─────────────┐
│  GB28181设备│
└─────┬───────┘
      │ SIP REGISTER (带认证)
      ▼
┌─────────────┐
│   eXosip2   │
└─────┬───────┘
      │ EXOSIP_MESSAGE_NEW
      ▼
┌─────────────┐
│  SipServer  │
└─────┬───────┘
      │ HandlerSipMessageNew()
      │ 1. 验证认证信息
      │ 2. 保存设备信息
      │ 3. 返回200 OK
      ▼
┌─────────────┐
│  DeviceDb   │
└─────┬───────┘
      │ 保存设备
      ▼
┌─────────────┐
│   eXosip2   │
└─────┬───────┘
      │ SIP 200 OK
      ▼
┌─────────────┐
│  GB28181设备│
└─────────────┘
```

---

## 🔐 安全设计

### 1. SIP认证

**认证方式**: Digest Authentication

**流程**:
1. 设备发送REGISTER（无认证）
2. 服务器返回401 Unauthorized（带nonce）
3. 设备计算响应值（MD5）
4. 设备发送REGISTER（带认证）
5. 服务器验证响应值
6. 验证通过返回200 OK

**实现**:
```cpp
// 生成nonce
std::string nonce = generateNonce();

// 计算HA1
DigestCalcHA1("MD5", username, realm, password, nonce, NULL, HA1);

// 计算Response
DigestCalcResponse(HA1, nonce, NULL, NULL, NULL, 0, method, uri, NULL, Response);

// 验证
if (memcmp(calc_response, Response, HASHHEXLEN) == 0) {
    // 认证成功
}
```

---

### 2. HTTP API安全

**计划实现**:
- JWT Token认证
- HTTPS支持
- API访问频率限制
- IP白名单

---

## 📊 性能设计

### 1. 并发处理

**线程模型**:
```
┌─────────────────────────────────────┐
│         主线程                       │
│  - HTTP服务器                        │
│  - 请求分发                          │
└─────────────────────────────────────┘
           │
           ├──────────────────────────┐
           │                          │
┌──────────▼──────────┐  ┌───────────▼──────────┐
│   SIP事件线程       │  │   工作线程池          │
│  - 处理SIP事件      │  │  - 业务逻辑处理       │
│  - 消息分发         │  │  - 数据库操作         │
└─────────────────────┘  └──────────────────────┘
```

**并发策略**:
- HTTP请求：oatpp异步处理
- SIP事件：单线程事件循环
- 业务逻辑：线程池处理
- 数据库：连接池管理

---

### 2. 性能指标

**目标**:
- HTTP API响应时间：< 100ms
- SIP消息处理延迟：< 50ms
- 并发设备数：> 1000
- 并发请求数：> 100 QPS

**优化措施**:
- 数据库索引优化
- 连接池复用
- 异步消息处理
- 内存缓存

---

## 🔧 可扩展性设计

### 1. 插件化架构

**Handler插件**:
```cpp
class SipMessageHandler {
public:
    virtual void handleMessage(eXosip_event_t* event) = 0;
    virtual void handleResponse(eXosip_event_t* event) = 0;
};

// 注册Handler
registerHandler("Catalog", std::make_shared<CatalogHandler>());
registerHandler("DeviceInfo", std::make_shared<DeviceInfoHandler>());
registerHandler("PTZ", std::make_shared<PTZHandler>());
```

---

### 2. 配置化设计

**配置文件**: `gbsip_server.yaml`
```yaml
sip:
  server_id: "34020000002000000001"
  server_ip: "192.168.1.100"
  server_port: 5060
  
http:
  port: 8003
  threads: 4
  
database:
  path: "db/gbsip.db"
  pool_size: 10
```

---

## 📈 监控设计

### 1. 日志系统

**日志级别**:
- ERROR: 错误信息
- WARNING: 警告信息
- INFO: 一般信息
- DEBUG: 调试信息

**日志内容**:
- SIP消息收发
- API请求响应
- 数据库操作
- 错误异常

---

### 2. 性能监控

**监控指标**:
- 在线设备数
- API请求量
- SIP消息量
- 数据库连接数
- 内存使用量
- CPU使用率

---

## 🔄 容错设计

### 1. 错误处理

**策略**:
- 参数验证
- 异常捕获
- 错误日志
- 友好提示

**实现**:
```cpp
try {
    // 业务逻辑
} catch (const std::exception& e) {
    SIPSERVER_LOG(LS_ERROR) << "Error: " << e.what();
    return createErrorResponse(500, e.what());
}
```

---

### 2. 超时处理

**超时设置**:
- SIP MESSAGE响应：10秒
- SIP INVITE响应：30秒
- HTTP API请求：60秒
- 数据库查询：5秒

---

## 📚 参考标准

1. **GB/T 28181-2016** - 公共安全视频监控联网系统信息传输、交换、控制技术要求
2. **RFC 3261** - SIP: Session Initiation Protocol
3. **RFC 4566** - SDP: Session Description Protocol
4. **RFC 3550** - RTP: A Transport Protocol for Real-Time Applications

---

**文档版本**: v1.0  
**创建时间**: 2026-02-09  
**维护者**: AI Assistant
